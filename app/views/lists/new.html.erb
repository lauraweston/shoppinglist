<div class="row">
  <div class="col-xs-12">
    <h1>New list</h1>
  </div>
</div>

  
    <%= form_for @list do |f| %>
      <% if @list.errors.any? %>
        <div id="error_explanation" class="alert alert-danger">
          <p>
            <strong>Oops!</strong> You have <%= pluralize(@list.errors.count, "error") %>:
          </p>
          <ul>
            <% @list.errors.full_messages.each do |msg| %>
              <li>
                <%= msg %>
              </li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= f.text_field :name, {:class => 'form-control', :placeholder => 'List name'} %>
      </div>

      <%= f.submit :class => 'btn btn-default'%>
    <% end %>  
    
    <div class="row">
      <div class="col-xs-12">
        <%= link_to 'Cancel', lists_path %>
      </div>
    </div>
    
  </div>
</div>